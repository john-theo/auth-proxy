{
    "name": "authproxy-tests",
    "version": "1.0.0",
    "description": "Unit tests for AuthProxy API service",
    "scripts": {
        "test": "jest",
        "test:watch": "jest --watch",
        "test:coverage": "jest --coverage"
    },
    "devDependencies": {
        "jest": "^29.7.0",
        "axios": "^1.6.0",
        "@types/jest": "^29.5.0"
    }
}